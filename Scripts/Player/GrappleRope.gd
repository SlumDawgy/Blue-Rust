extends Line2D

#var player : Player

# Called when the node enters the scene tree for the first time.
#func _ready():
	#top_level = true
#
## Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(_delta):
	#if get_point_position(0) != player.grappleOrigin.to_global(position):
		#set_point_position(0, player.grappleOrigin.to_global(position))
	#if get_point_position(1) != owner.to_global(position):
		#set_point_position(1, owner.to_global(position))
	#pass
